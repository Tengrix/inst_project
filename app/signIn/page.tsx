import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Sign In',
}

const SignIn = () => {
    return (
        <h1>
            Sign In
        </h1>
    );
};

export default SignIn;